# Copy this file to .env and fill in your real connection string.
# For Neon (Postgres): change provider in prisma/schema.prisma to "postgresql" first.
# For TiDB Cloud (MySQL): keep provider as "mysql".
DATABASE_URL="mysql://user:password@host:4000/notice_board?sslaccept=strict"
